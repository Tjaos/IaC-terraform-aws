# IaC-terraform-aws

Ao clonar o projeto, lembre-se de gerar uma chave ssh com o comando ```ssh-keygen``` escolhendo o diretório (de preferência dentro da pasta 'env/Dev' | 'env/Prod' - vai depender do uso)

